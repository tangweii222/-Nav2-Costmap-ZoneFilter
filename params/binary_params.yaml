costmap_filter_info_server:
  ros__parameters:
    use_sim_time: true
    type: 3  # 這裡設為 Binary filter 類型
    filter_info_topic: "/zone_filter_info"
    mask_topic: "/zone_filter_mask"
    base: 0.0
    multiplier: 1.0

filter_mask_server:
  ros__parameters:
    use_sim_time: true
    frame_id: "map"
    topic_name: "/zone_filter_mask"
    yaml_filename: "zone_mask.yaml"
